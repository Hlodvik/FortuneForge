# Vendored client package feed

This directory contains immutable npm tarballs produced by the separate reusable-games repository.
Application dependencies must reference an exact local file artifact and record its SHA-256 in
MANIFEST.sha256. The Asteroids artifact below is a supervised working-tree candidate, not a
source-commit release.
